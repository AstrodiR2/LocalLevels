name: Build LocalLevels

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build with Geode
        id: build
        uses: geode-sdk/build-geode-mod@main
        with:
          combine: true
          sdk: v5.6.1

      - uses: actions/upload-artifact@v4
        with:
          name: LocalLevels
          path: output/*.geode
